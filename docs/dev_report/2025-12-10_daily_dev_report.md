# 📝 2025-12-10 개발 일지

## 1. 📋 개요

| 항목 | 내용 |
|------|------|
| 📅 **날짜** | 2025-12-10 (화) |
| 🎯 **진행 작업** | Self-Hosting 마이그레이션, Google 로그인 구현, Vite 빌드 캐시 문제 해결 |
| 📦 **커밋 내역** | (로컬 작업 진행 중, 원격 푸시 전) |

### 📌 요약
Vercel 환경에서 벗어나 자체 Linux 서버(Express + PM2 + Nginx)로 호스팅 환경을 성공적으로 이관했습니다. 기존의 테스트용 고정 계정 방식을 폐기하고 Firebase Google Authentication을 연동하여 실제 사용자 로그인을 구현했습니다. 개발 과정에서 발생한 Vite 빌드 캐시 미반영 문제를 분석하고 해결 스크립트를 추가하여 배포 안정성을 확보했습니다.

---

## 2. 🔧 진행 작업

### 2.1 🏠 Self-Hosting 마이그레이션

#### 🎯 목적
Vercel의 관리형 호스팅 서비스 의존도를 낮추고, 자체 Linux 서버에서 Node.js 애플리케이션을 직접 제어하여 운영 비용을 절감하고 커스터마이징 자유도를 확보하기 위함입니다.

#### 📝 변경사항
1. **Express 서버 구축 (`server.js`)**: 기존 Vercel이 처리하던 정적 파일 서빙과 SPA 라우팅을 처리하기 위해 Express 서버를 구축했습니다. `compression` 미들웨어를 사용하여 전송 데이터를 압축, 로딩 속도를 개선했습니다.
2. **PM2 설정 (`ecosystem.config.cjs`)**: 애플리케이션의 무중단 서비스를 위해 프로세스 매니저인 PM2 설정 파일을 생성했습니다. 이를 통해 서버 재부팅 시 자동 실행과 에러 발생 시 자동 재시작이 가능해졌습니다.
3. **Nginx 리버스 프록시 설정 (`nginx.conf`)**: 80/443 포트로 들어오는 외부 요청을 내부 3000번 포트로 전달하는 리버스 프록시를 구성했습니다. 또한 Certbot을 연동하여 HTTPS 보안 연결을 적용했습니다.

#### 📂 변경 파일
- `server.js` (신규): Express 서버 진입점
- `ecosystem.config.cjs` (신규): PM2 관리 설정
- `nginx.conf` (신규): Nginx 설정 템플릿
- `package.json` (수정): 서버 실행 스크립트 추가

#### 🚀 후속 작업
1. **CI/CD 파이프라인 구축**
   - GitHub Actions를 사용하여 코드 푸시 시 자동으로 테스트하고 운영 서버에 배포되는 파이프라인을 구축하여 수동 배포의 번거로움과 실수를 방지해야 합니다.
2. **서버 리소스 모니터링 시스템 도입**
   - PM2 모니터링 또는 Grafana 같은 도구를 도입하여 CPU, 메모리 사용량을 실시간으로 추적하고 장애 발생 시 알림을 받을 수 있는 환경을 마련해야 합니다.
3. **로그 관리 및 로테이션 설정**
   - 서버 로그가 무한정 쌓여 디스크 용량을 차지하는 것을 방지하기 위해 `pm2-logrotate` 모듈을 설치하여 로그 파일을 주기적으로 분할하고 정리해야 합니다.
4. **Nginx 캐싱 최적화**
   - 정적 자산(이미지, JS, CSS)에 대한 브라우저 캐싱 정책을 Nginx 설정에 추가하여 클라이언트의 로딩 속도를 높이고 서버 트래픽 부하를 줄여야 합니다.
5. **데이터베이스 백업 자동화**
   - 자체 호스팅 환경의 안정성을 위해 Firebase Firestore 데이터를 주기적으로 백업하는 스크립트를 작성하고 크론잡(Cronjob)으로 스케줄링해야 합니다.

---

### 2.2 🔐 Google 로그인 기능 구현

#### 🎯 목적
단일 테스트 계정(`tester@vocaloop.ai`)을 사용하는 기존 방식은 다중 사용자 지원이 불가능하므로, 실제 사용자가 자신의 Google 계정으로 로그인하여 개인화된 단어장을 관리할 수 있도록 인증 시스템을 개편했습니다.

#### 📝 변경사항
1. **Firebase 인증 로직 변경 (`App.jsx`)**: 기존의 하드코딩된 이메일/비밀번호 로그인 로직을 제거하고, `signInWithPopup`과 `GoogleAuthProvider`를 사용하여 팝업 기반의 소셜 로그인 흐름으로 완전히 대체했습니다.
2. **로그인 화면 컴포넌트 추가 (`LoginScreen.jsx`)**: 비로그인 사용자가 처음 마주하게 될 직관적인 로그인 인터페이스를 신규 개발했습니다. "Google로 계속하기" 버튼을 배치하여 진입 장벽을 낮췄습니다.
3. **헤더 사용자 UI 개선 (`Header.jsx`)**: 로그인 상태일 때 사용자의 프로필 사진이나 이니셜(사진이 없을 경우)을 표시하고, 명시적인 로그아웃 버튼을 추가하여 계정 관리의 편의성을 높였습니다.

#### 📂 변경 파일
- `src/components/LoginScreen.jsx` (신규): 로그인 페이지 UI
- `src/App.jsx` (수정): 인증 상태 관리 및 라우팅 로직 변경
- `src/components/Header.jsx` (수정): 프로필 UI 및 로그아웃 핸들러 연결
- `src/components/Icons.jsx` (수정): UI에 필요한 아이콘 추가

#### 🚀 후속 작업
1. **로그인 세션 지속성 설정**
   - `setPersistence` 옵션을 명시적으로 설정하여 브라우저를 닫거나 새로고침해도 로그인 상태가 안정적으로 유지되도록 하여 사용자 경험을 향상시켜야 합니다.
2. **사용자 최초 로그인 시 초기 데이터 세팅 로직 최적화**
   - 신규 가입자가 처음 로그인했을 때 튜토리얼용 샘플 단어장을 생성해주는 로직을 최적화하여 온보딩 경험을 부드럽게 만들어야 합니다.
3. **프로필 설정 페이지 개발**
   - 사용자가 자신의 닉네임이나 학습 선호 설정을 변경할 수 있는 별도의 프로필 페이지를 개발하여 개인화 기능을 강화해야 합니다.
4. **다양한 소셜 로그인 제공업체 추가**
   - Google 외에도 GitHub나 Apple 로그인 등 다양한 인증 수단을 추가하여 사용자의 선택권을 넓히고 서비스 접근성을 높여야 합니다.
5. **회원 탈퇴 기능 구현**
   - 개인정보 보호법 준수 및 사용자 권리 보장을 위해 사용자가 원할 때 계정과 모든 데이터를 영구적으로 삭제할 수 있는 기능을 구현해야 합니다.

---

### 2.3 🐛 Vite 빌드 캐시 문제 해결

#### 🎯 목적
코드를 수정했음에도 불구하고 프로덕션 빌드 결과물에 변경 사항이 반영되지 않아 배포된 서비스의 UI가 갱신되지 않는 치명적인 버그를 해결하기 위함입니다.

#### 📝 변경사항
1. **빌드 스크립트 개선 (`package.json`)**: Vite가 `node_modules/.vite`에 캐시된 의존성을 계속 재사용하는 것이 원인임을 파악했습니다. 이를 해결하기 위해 빌드 전 캐시 디렉토리와 결과물 폴더를 삭제하는 `build:clean` 스크립트를 추가했습니다.
2. **문제 상황 문서화 (`docs/troubleshooting`)**: 향후 동일한 문제가 발생했을 때 빠르게 대응할 수 있도록 증상, 원인, 해결 방법을 상세하게 기록한 트러블슈팅 문서를 작성했습니다.

#### 📂 변경 파일
- `package.json` (수정): `clean`, `build:clean` 스크립트 추가
- `docs/troubleshooting/issue_vite_빌드_캐시_미반영.md` (신규): 장애 대응 보고서

#### 🚀 후속 작업
1. **GitHub Actions 워크플로우에 캐시 정리 단계 추가**
   - CI/CD 파이프라인 구성 시 빌드 단계에서 캐시로 인한 문제가 발생하지 않도록 명시적으로 클린 빌드를 수행하는 단계를 포함시켜야 합니다.
2. **로컬 개발 환경의 캐시 관리 가이드 작성**
   - 개발자들이 로컬에서 `npm run dev` 실행 시 겪을 수 있는 유사한 캐시 문제에 대응할 수 있도록 `README.md`에 트러블슈팅 가이드를 추가해야 합니다.
3. **빌드 결과물 해시 변경 검증 스크립트 작성**
   - 배포 전에 소스 코드 변경 시 빌드된 JS 파일의 해시값이 실제로 변경되었는지 자동으로 체크하는 간단한 스크립트를 작성하여 배포 실수를 방지해야 합니다.
4. **Vite 설정 최적화 검토**
   - Vite 설정 파일(`vite.config.js`)에서 빌드 캐시 전략을 검토하고, 필요하다면 캐시 무효화 전략을 더 공격적으로 설정할 수 있는지 확인해야 합니다.
5. **배포 후 브라우저 캐시 무효화 전략 수립**
   - 서버 측 Nginx 설정이나 HTML 메타 태그를 통해 클라이언트 브라우저가 오래된 JS/CSS 파일을 캐싱하지 않고 새 버전을 받아오도록 설정해야 합니다.

---

## 3. 📊 현재 상태

### 🏗️ 프로젝트 구조 분석
현재 프로젝트는 `/src` 폴더 내에 컴포넌트, 페이지 로직, 스타일이 집중된 전형적인 Vite React 구조를 따르고 있습니다. `/docs` 폴더를 통해 개발 문서와 트러블슈팅 내역을 체계적으로 관리하기 시작했으며, 루트 디렉토리에 서버 관련 설정(`server.js`, `nginx.conf`)이 추가되어 프론트엔드와 백엔드(서빙용)가 공존하는 형태가 되었습니다.

### ✅ 현재 구현된 기능 및 방향성
- **구현됨:** 자체 서버 호스팅, HTTPS 보안 연결, Google 소셜 로그인, 기본적인 단어장 CRUD(AI 분석 포함).
- **방향성:** 이제 인프라와 인증이라는 기반 공사가 끝났으므로, 실제 서비스의 핵심 가치인 '학습 경험'을 고도화하는 단계로 넘어갈 준비가 되었습니다.

### 🎯 전체 프로젝트 관점의 향후 작업
1. **퀴즈 및 학습 모드 구현 (Core Feature)**
   - 저장된 단어를 기반으로 플래시카드나 사지선다 퀴즈를 제공하는 학습 모드를 구현하여 단순 저장소가 아닌 학습 도구로서의 가치를 제공해야 합니다.
2. **반응형 UI/UX 디자인 고도화**
   - 현재 기능 중심으로 구현된 UI를 다듬어, 모바일과 데스크톱 모두에서 매끄럽고 심미적으로 만족스러운 사용자 경험을 제공하도록 CSS 스타일링을 강화해야 합니다.
3. **데이터베이스 구조 최적화 및 인덱싱**
   - 단어 데이터가 쌓일 것을 대비하여 Firestore 쿼리 효율을 높이기 위한 복합 인덱스를 설정하고, 데이터 읽기 비용을 최적화하는 구조로 리팩토링해야 합니다.
4. **전역 상태 관리 라이브러리 도입 검토**
   - 현재 `App.jsx`에 집중된 상태 관리가 복잡해지고 있으므로, Zustand나 Recoil 같은 경량 상태 관리 라이브러리를 도입하여 데이터 흐름을 체계화해야 합니다.
5. **테스트 코드 커버리지 확대**
   - 주요 비즈니스 로직(단어 분석, 퀴즈 정답 처리 등)에 대한 단위 테스트를 작성하여 기능 추가 시 발생할 수 있는 사이드 이펙트를 최소화해야 합니다.

---

## 4. ⚠️ 개선사항

### 🐛 잠재적 버그 및 우려사항
- **Vercel 종속 패키지 잔재**: `package.json`에 `@vercel/speed-insights`가 여전히 남아있어 불필요한 용량을 차지하고 있습니다.
- **번들 사이즈 경고**: 빌드 시 500KB가 넘는 청크 경고가 발생하고 있어, 초기 로딩 속도 저하가 우려됩니다. 코드 스플리팅이 필요합니다.

### 🔄 리팩토링 제안
- **Auth 로직 분리**: `App.jsx`에 Firebase 초기화, Auth 상태 감지, DB 스냅샷 리스너가 섞여 있어 코드가 비대합니다. 이를 `AuthContext`나 별도 훅(`useAuth`)으로 분리하면 유지보수성이 크게 향상될 것입니다.
- **상수 관리**: `TEST_EMAIL` 등은 삭제했지만, 여전히 산재된 설정값들을 `src/constants/` 디렉토리로 모아서 관리하는 것이 좋습니다.

### 📁 구조 개선 제안
- **Config 분리**: `firebaseConfig` 관련 로직을 `src/config/firebase.js`로 분리하여 모듈화해야 합니다.
- **Service 레이어 도입**: Firestore와 통신하는 로직을 컴포넌트에서 분리하여 `src/services/wordService.js` 등으로 캡슐화하는 정석적인 패턴 도입을 제안합니다.

---

## 5. 📊 총평

| 항목 | 내용 |
|------|------|
| 🏆 **주요 성과** | 관리형 호스팅에서 벗어나 **자체 서버 구축** 성공, **실 사용자 인증 시스템** 도입 완료 |
| 📈 **진행률** | 인프라 및 기반 기능(Phase 1) 100% 완료, 학습 기능(Phase 2) 진입 준비 완료 |
| 🚧 **장애 해결** | Vite 빌드 캐시 문제 해결을 통한 **배포 안정성 확보** |
| 📝 **한줄 평** | "서버의 주인이 되었고, 실제 사용자를 맞이할 문을 열었다." |

---

> 📅 작성일: 2025-12-10  
> 👤 작성자: AI Assistant
